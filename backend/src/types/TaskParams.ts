import { By } from "./By";

// URL Params for HTTP GET requests
export enum TaskSortParams{
    SORT_BY="sortBy",
    SORT_ORDER="sortOrder",
}

export enum TaskPaginateParams{
    PAGE="page",
    PAGE_SIZE="pageSize"
}